import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./src/**/*.{html,ts,js}", // 프로젝트 파일 경로
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
